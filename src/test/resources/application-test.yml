spring:

  datasource:
      url: jdbc:h2:mem:testdb
      username: sa
      password: password


  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

  flyway:
    create-schemas: true
    schemas: dev_michael
    enabled: true

#logging:
#  level:
#    root: debug